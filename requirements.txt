# ==============================================================================
# AI Interview Proctor - Direct Dependencies Only
# ==============================================================================
# Only list packages your code directly imports. pip will auto-install their
# transitive dependencies (numpy, protobuf, pillow, etc.) at compatible versions.
#
# Pinned to versions tested on Python 3.11 for Streamlit Cloud compatibility.
# ==============================================================================

# --- Core Framework ---
streamlit>=1.30.0

# --- Computer Vision ---
opencv-python-headless>=4.8.0      # Headless build: no GUI deps, works on Linux servers
mediapipe>=0.10.9                   # Face Detection + Face Mesh

# --- Object Detection ---
ultralytics>=8.0.0                  # YOLOv8 (pulls torch automatically)

# --- Data & Reporting ---
pandas>=2.0.0
numpy<2.0.0
